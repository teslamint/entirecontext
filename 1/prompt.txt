Independently verify only two follow-up corrections in the current uncommitted diff. Do not edit files. Do not run tests, linters, formatters, builds, or network commands. First, src/entirecontext/core/decision_file_lineage.py replaced one git merge-base subprocess per rename transition with one git rev-list --parents HEAD batch plus in-memory ancestor traversal under the shared deadline; verify exact Git ancestry, same-HEAD replay, branch/path-reuse behavior, timeout behavior, and no missed reachable transition. Second, scripts/validate_plan.py now tracks nested Markdown list levels so deeply nested list-continuation prose is scanned for inline shell commands while genuine indented code blocks remain excluded; verify nested siblings, outdents, and code boundaries. Inspect their new tests and immediate blast radius. Report only evidence-backed P0-P2 bugs with exact file:line and concrete failing input. Return CLEAN if both corrections are sound.