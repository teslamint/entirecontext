<task>Review only the current uncommitted decision-file rename lineage feature against its approved Spec, ADR, and Plan. Read docs/specs/2026-08-17-decision-file-rename-lineage-design.md, docs/adr/0015-decision-file-rename-lineage.md, docs/superpowers/plans/2026-08-17-007-decision-file-rename-lineage-plan.md; src/entirecontext/core/decision_file_lineage.py, src/entirecontext/db/migrations/v019.py, src/entirecontext/db/migrations/__init__.py, src/entirecontext/db/schema.py, src/entirecontext/hooks/decision_hooks.py, src/entirecontext/hooks/handler.py; tests/test_decision_file_lineage.py, tests/test_migration_v019.py, tests/test_db_schema.py, tests/test_decision_hooks.py, tests/test_handler.py; ROADMAP.md, CHANGELOG.md, CLAUDE.md. Exclude LESSONS.md, .entire/, .opencode/, and unrelated pre-existing issues. Read-only: do not modify files or run tests, builds, formatters, or network.</task><grounding_rules>Trace concrete control and data paths before reporting. Check parser framing, Git ancestry/error semantics, schema v19 parity and strict replay, recursive propagation/cycles, transaction rollback, first/incremental/force-reset scans, SessionStart-before-ranking, PostToolUse no-Git, fail-open warnings and total latency budget, unchanged public query behavior, requirements completeness, and mutation-resistant coverage. Every finding must cite exact file:line and a reproducible failure path. Omit style preferences and low-confidence hypotheses.</grounding_rules><structured_output_contract>Return findings ordered P0-P3, each with title, file:line, failing path, impact, and minimal fix. Return CLEAN if no P0-P2 actionable issue remains.</structured_output_contract><dig_deeper_nudge>After the first plausible issue, inspect second-order empty-state, retry, stale-watermark, force-push, malformed-output, rollback, and cyclic-lineage paths.</dig_deeper_nudge>

---

<task>Re-review only the six original findings against the current files after author fixes. Verify: merge commits use separate-parent diffs with a real merge-resolution test; truncated NUL output is rejected; DB failure is injected after lineage insertion and rollback/retry asserted; cyclic lineage has set-semantics coverage; PostToolUse guards synchronizer, Git-root discovery, and subprocess.run; CLAUDE.md documents ownership/invariants; propagation normalizes both operands for literal backslashes. The missing-repository warning finding was intentionally rejected because non-Git SessionStart is a normal no-op, while actual sync failures already record a warning.</task><grounding_rules>Read only current relevant source/tests/docs. Do not edit or run tests/builds/network. Keep a finding only if a concrete P0-P2 defect remains, with exact file:line and failure path.</grounding_rules><structured_output_contract>Return CLEAN if all original actionable findings are resolved and the warning pushback is technically sound; otherwise return only surviving/new P0-P2 findings.</structured_output_contract>