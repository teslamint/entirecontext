Review only the current uncommitted correction patch for PR #226 and immediate blast radius. Do not edit files. Do not run tests, formatters, linters, builds, or network commands. Governing contract: docs/specs/2026-08-17-decision-file-rename-lineage-design.md. Focus on src/entirecontext/core/decision_file_lineage.py, schema migrations v19-v21, src/entirecontext/core/decisions.py, scripts/validate_plan.py, their tests, CHANGELOG.md, and ADR 0015. Verify: (1) one git rev-list --parents HEAD plus early-exit per-pair graph searches only accepts exact reachable ancestor relations, handles branches/merges/unreachable stale commits, remains under the shared deadline, and preserves same-HEAD replay; (2) Markdown prose scanning rejects inline shell commands in lazy and deeply indented list continuations but permits them only inside genuine CommonMark indented code blocks beginning at a valid block boundary; (3) only the first contiguous Spec Test Disposition table is accepted and later pipe rows fail closed; (4) v21 materialization provenance migration is strict, transactional, fresh-schema equivalent, and does not seed later propagation from derived rows. Important contract interpretation: an old chronologically valid a->b->c chain remains historical lineage even if a later a->b reuse occurs; spec lines 14,49,97,167 require preserving all historical destinations, so do not flag that preservation unless a non-ancestor transition is traversed. Report only evidence-backed P0-P2 defects with exact file:line and concrete failing input/path. Return CLEAN if none.